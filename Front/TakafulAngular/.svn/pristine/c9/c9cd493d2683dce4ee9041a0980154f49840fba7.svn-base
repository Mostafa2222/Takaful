import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LocationDefinitionComponent } from './location-definition.component';

describe('LocationDefinitionComponent', () => {
  let component: LocationDefinitionComponent;
  let fixture: ComponentFixture<LocationDefinitionComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [LocationDefinitionComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(LocationDefinitionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
