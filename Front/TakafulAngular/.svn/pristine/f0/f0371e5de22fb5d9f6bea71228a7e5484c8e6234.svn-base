import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RelatedMapComponent } from './related-map.component';

describe('RelatedMapComponent', () => {
  let component: RelatedMapComponent;
  let fixture: ComponentFixture<RelatedMapComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [RelatedMapComponent]
    })
    .compileComponents();
    
    fixture = TestBed.createComponent(RelatedMapComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
